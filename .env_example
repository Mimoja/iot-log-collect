# ============================================================
# Credentials for the EFK stack
# Change these before deploying to production!
# ============================================================

ELASTIC_PASSWORD=changeme
KIBANA_PASSWORD=changeme

# ============================================================
# TLS certificates for the Fluentd edge device forward input
# Point this to the directory containing server.crt and server.key
# For Let's Encrypt, use the live directory directly:
#   FLUENTD_CERT_DIR=/etc/letsencrypt/live/sentry.meticulousespresso.com
# The container expects:
#   server.crt  (or fullchain.pem symlinked to server.crt)
#   server.key  (or privkey.pem symlinked to server.key)
# ============================================================
FLUENTD_CERT_DIR=./fluentd/certs
