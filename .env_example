# ============================================================
# Credentials — change these before deploying to production!
# ============================================================

# Grafana
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme

# ============================================================
# TLS certificates for the OTel Collector OTLP/HTTP receiver
# The OTel Collector expects Let's Encrypt certs mounted at:
#   /etc/letsencrypt/live/sentry.meticulousespresso.com/fullchain.pem
#   /etc/letsencrypt/live/sentry.meticulousespresso.com/privkey.pem
# The host's /etc/letsencrypt is bind-mounted into the container.
# ============================================================
